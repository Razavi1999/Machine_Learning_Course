\begin{boxK}
 \begin{enumerate}
     \item 
    بدون احتساب هزینه زمانی فشرده‌کردن فایل ، زمان نهایی برابر خواهد بود با حجم فایل تقسیم بر پهنای باند شبکه.

    \lr{totalTime = fileSize / bandwidth}

    هم اکنون خواهیم داشت : 

    \lr{totalTime = compressionTime + fileSize/bandwidth}

    \lr{
    bandWidth = compressFileSize / compressTime
    }

    حالت اول :
    
    \lr{0.5MB / 1 sec = 0.5 MB/sec}

    حالت دوم :
    
    \lr{
    0.6MB / 2 sec = 0.3 
    MB/ sec
    }
 \item 

    تاخیر بر روی پاسخ تاثیری ندارد ، زیرا تاثیر آن بر روی فایل فشرده‌شده و غیرفشرده یکسان است.
 \end{enumerate}
\end{boxK}